# PostgreSQL connection. In Vercel, add Neon Postgres from the project's
# Storage tab and both vars are auto-injected.
#   DATABASE_URL          = pooled URL (pgbouncer) → used by the app at runtime
#   DATABASE_URL_UNPOOLED = direct URL             → used by `prisma migrate`
DATABASE_URL="postgres://user:pass@host/db?sslmode=require&pgbouncer=true"
DATABASE_URL_UNPOOLED="postgres://user:pass@host/db?sslmode=require"

# REQUIRED in production. Generate with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
SESSION_SECRET="change-me-to-a-long-random-string"

# Set to "0" in production to disable demo sign-in.
ALLOW_DEMO_AUTH="1"
